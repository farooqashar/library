extends layout 

block content 
    h1=title 

    ul 
        each genre in all_genres 
            li 
                a(href=genre.url) #{genre.name} 

        else 
            li There are currently no genres.