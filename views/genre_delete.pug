extends layout 

block content 
    h1= title 

    if genre_books.length

        p.text-warning #[strong Please delete the following books before attempting to delete this genre.]

        div(style='margin-left:20px;margin-top:20px')

            h4 Books
            dl
            each book in genre_books
                dt
                    a(href=book.url) #{book.title}
                dd

    else
        h4.text-danger Do you really want to delete this genre? 
        p.text-danger This action cannot be undone.


        form(method='POST' action='')
            div.form-group
                input#genreid.form-control(type='hidden',name='genreid', required='true', value=genre._id )

            button.btn.btn-danger(type='submit') Delete Genre